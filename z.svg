<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg"
     xmlns:xlink="http://www.w3.org/1999/xlink"
     viewBox="0 0 800 600"
     width="100%" height="100%">

  <title>HTML Injector</title>

  <foreignObject x="0" y="0" width="100%" height="100%">
    <body xmlns="http://www.w3.org/1999/xhtml" style="margin:0;padding:0;width:100%;height:100%;overflow:hidden;">
      <iframe id="f" style="width:100%;height:100vh;border:none;display:block;"></iframe>
    </body>
  </foreignObject>

  <script type="text/javascript">
  <![CDATA[
  (function() {

    var URLS = [
      "https://raw.githack.com/darkdragonzxs/zd-pages/main/zodiac.html",
      "https://raw.githubusercontent.com/darkdragonzxs/zd-pages/main/zodiac.html",
      "https://cdn.statically.io/gh/darkdragonzxs/zd-pages@main/zodiac.html",
      "https://cdn.jsdelivr.net/gh/Darkdragonzxs/zd-pages@latest/zodiac.html"
    ];

    var frame = document.getElementById("f");

    function fill() {
      frame.style.width  = window.innerWidth  + "px";
      frame.style.height = window.innerHeight + "px";
    }

    window.addEventListener("resize", fill);
    fill();

    function tryFetch(i) {
      if (i >= URLS.length) return;
      fetch(URLS[i])
        .then(function(r) {
          if (!r.ok) throw new Error();
          return r.text();
        })
        .then(function(html) {
          var blob = new Blob([html], { type: "text/html" });
          frame.src = URL.createObjectURL(blob);
        })
        .catch(function() { tryFetch(i + 1); });
    }

    tryFetch(0);

  }());
  ]]>
  </script>

</svg>
