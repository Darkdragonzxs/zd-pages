<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg"
     xmlns:xlink="http://www.w3.org/1999/xlink"
     viewBox="0 0 800 600"
     width="800" height="600">

  <title>HTML Injector</title>

  <rect width="800" height="600" fill="#0a0a0f"/>
  <text x="400" y="300" text-anchor="middle"
        font-family="monospace" font-size="13" fill="#555" id="status">
    Loading...
  </text>

  <foreignObject x="0" y="0" width="800" height="600">
    <body xmlns="http://www.w3.org/1999/xhtml" style="margin:0;padding:0;">
      <iframe id="f" style="width:800px;height:600px;border:none;display:block;"></iframe>
    </body>
  </foreignObject>

  <script type="text/javascript">
  <![CDATA[
  (function() {

    var URLS = [
      "https://raw.githubusercontent.com/darkdragonzxs/zd-pages/main/zodiac.html",
      "https://cdn.jsdelivr.net/gh/darkdragonzxs/zd-pages@main/zodiac.html",
      "https://raw.githack.com/darkdragonzxs/zd-pages/main/zodiac.html"
    ];

    var status = document.getElementById("status");
    var frame  = document.getElementById("f");

    function tryFetch(i) {
      if (i >= URLS.length) {
        status.textContent = "All URLs failed.";
        return;
      }
      fetch(URLS[i])
        .then(function(r) {
          if (!r.ok) throw new Error("HTTP " + r.status);
          return r.text();
        })
        .then(function(html) {
          var blob = new Blob([html], { type: "text/html" });
          frame.src = URL.createObjectURL(blob);
          status.textContent = "";
        })
        .catch(function() {
          tryFetch(i + 1);
        });
    }

    tryFetch(0);

  }());
  ]]>
  </script>

</svg>
